


ckpt:
  path: ${path.glob_model_path}/${data.dataname}/bb_latn_16_n_icnn_test
  full_resume: True

data:
  name: resume_bb_latn_16_n_icnn_test_why_crashing

train:
  f_per_g: 4
  g_per_f: 10
  optimizer_args_f:
    name: adamw
    args:
      betas: [0.9, 0.99]
      lr: 0.0001
      weight_decay: 0.01

  optimizer_args_g:
    name: adamw
    args:
      betas: [0.9, 0.99]
      lr: 0.0001
      weight_decay: 0.01


eval:
  plot_figures: 5
